[project]
name = "hs-osint"
version = "0.1.0"
description = "A safe, extensible OSINT search and analysis CLI."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "HS_OSINT contributors" }
]
dependencies = []

[project.scripts]
hs-osint = "hs_osint.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
